<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iggyapp.qboad">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_qboad"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QBoad">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".FolowerActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".TopActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".UserPageActivity" />
        <activity android:name=".PostingRegisterActivity" />
        <activity android:name=".PostingActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity" />
    </application>

</manifest>